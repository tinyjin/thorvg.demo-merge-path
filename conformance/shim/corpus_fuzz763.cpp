/* One skia pathops case file, compiled against the shim in tests/ so that every
   testPathOp() in it records its operands instead of solving them. Nothing here
   but the include - the geometry is skia's, untouched. */
#include <PathOpsFuzz763Test.cpp>
